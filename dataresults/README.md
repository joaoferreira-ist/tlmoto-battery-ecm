# Data

The CSV files contain battery test-cycle measurements with voltage, current,
temperature, and time columns. The included `00001.csv` is a small example
used by the optional validation command.

The data should only be redistributed on GitHub after confirming the original
dataset license and attribution requirements. Keep larger or restricted raw
data outside the repository and document how to obtain it here.